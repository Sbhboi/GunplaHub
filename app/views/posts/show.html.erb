<h1><%= @post.title %></h1>
    <% if @post.image.attached? %>
        <p><%= @post.content %></p>
        <%= image_tag @post.image.variant(resize_to_limit: [300, 300]) %>
    <% else %>
        <p>No image available</p>
    <% end %>
   